# Server-only. Never use a VITE_ prefix for credentials.
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-2603
# Set true only after verifying this key belongs to a Free-mode account with
# pay-as-you-go disabled. The API cannot determine the account's billing plan.
MISTRAL_FREE_TIER_CONFIRMED=false
